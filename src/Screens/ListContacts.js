import { View, Text } from 'react-native';
import MasterView from '../Shared/MasterView';

const ListContacts = ({ navigation }) => {
  return (
    <MasterView>
      <View>
        <Text>Hello from ListContacts</Text>
      </View>
    </MasterView>
  )
}

export default ListContacts;
