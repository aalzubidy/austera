import { View, Text } from 'react-native';
import MasterView from '../Shared/MasterView';

const SearchPeople = ({ navigation }) => {
  return (
    <MasterView>
      <View>
        <Text>Hello from SearchPeople</Text>
      </View>
    </MasterView>
  )
}

export default SearchPeople;
