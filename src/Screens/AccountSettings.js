import { View, Text } from 'react-native';
import MasterView from '../Shared/MasterView';

const AccountSettings = ({ navigation }) => {
  return (
    <MasterView>
      <View>
        <Text>Hello from AccountSettings</Text>
      </View>
    </MasterView>
  )
}

export default AccountSettings;
