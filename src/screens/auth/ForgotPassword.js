import { View, Button, Text, Animated } from 'react-native';

const ForgotPassword = ({ navigation }) => {
  return (
    <View>
      <Text>Hello from forgot password page</Text>
    </View>
  )
};

export default ForgotPassword;
